import 'package:flutter/material.dart';

class ReadCompMedium extends StatelessWidget {
  const ReadCompMedium({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Easy Reading Comprehension')),
      body: const Center(child: Text('Easy Modules List Here')),
    );
  }
}