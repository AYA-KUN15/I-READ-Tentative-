import 'package:flutter/material.dart';

class ReadCompHard extends StatelessWidget {
  const ReadCompHard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hard Reading Comprehension')),
      body: const Center(child: Text('Hard Modules List Here')),
    );
  }
}